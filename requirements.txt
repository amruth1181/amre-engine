fastapi
uvicorn[standard]
pydantic
python-multipart
python-dotenv
httpx
requests
PyJWT
bcrypt
passlib[bcrypt]
numpy
scikit-learn
sympy
# PRM floor: Skywork 1.5B runs in native PyTorch (custom arch can't ONNX-export);
# int8 dynamic quantization is applied at load time on CPU.
# NOTE: torch is installed separately in the Dockerfile from the CPU-only index
# (the default CUDA wheel is ~2.5GB and breaks the free CPU Space). Do not add
# `torch` here or pip will pull the CUDA build.
# transformers PINNED to 4.44.2: Skywork's vendored modeling code targets ~4.44.
# Newer transformers changed the attention-mask utils and removed
# DynamicCache.from_legacy_cache, which made the forward emit NaN hidden states
# (every PRM step scored 0.5). Pinning makes the custom code run as designed.
transformers==4.44.2
huggingface_hub
accelerate
# Persistent DB: Turso/libSQL (SQLite-compatible) so per-user data survives Space
# restarts — the free Space has no persistent disk. Enabled when TURSO_DATABASE_URL
# is set (app/db.py); falls back to local sqlite3 otherwise.
libsql-experimental
# OCR (§3.8) — Pillow stays (tiny). pix2tex is DISABLED: it pulls torchvision +
# opencv and breaks the free CPU Space build. OCR is the first cut item (§7); the
# /ocr endpoint already returns 503 gracefully when pix2tex is absent. Re-enable
# by uncommenting once the Space has headroom.
Pillow
# pix2tex
# study-sheet PDF export (§9.9) — fpdf2 is pure-Python (no system deps, unlike weasyprint)
fpdf2
# dev/test (§6)
pytest
